const C={'access-control-allow-origin':'*','access-control-allow-methods':'GET,POST,OPTIONS','access-control-allow-headers':'Content-Type,X-Admin-Token','cache-control':'no-store'};
const S=v=>String(v??'').trim();const ID=p=>`${p}_${crypto.randomUUID()}`;const NOW=()=>new Date().toISOString();const J=(x,s=200,h={})=>new Response(JSON.stringify(x),{status:s,headers:{'content-type':'application/json;charset=UTF-8',...C,...h}});
function auth(r,e,d){const supplied=S(r.headers.get('X-Admin-Token')||d.token),expected=S(e.REQOO_ADMIN_TOKEN||e.SHOP_ADMIN_TOKEN||e.ADMIN_KEY);return !!supplied&&supplied===expected}
async function body(r){const u=new URL(r.url);if(r.method==='GET')return Object.fromEntries(u.searchParams.entries());try{return await r.json()}catch{return {}}}
async function ensure(e){await e.DB.batch([e.DB.prepare("CREATE TABLE IF NOT EXISTS shipping_methods(id TEXT PRIMARY KEY,name TEXT NOT NULL,price_minor INTEGER NOT NULL DEFAULT 0,active INTEGER NOT NULL DEFAULT 1,sort_order INTEGER NOT NULL DEFAULT 0,created_at TEXT NOT NULL,updated_at TEXT NOT NULL)"),e.DB.prepare("CREATE TABLE IF NOT EXISTS promotions(id TEXT PRIMARY KEY,code TEXT NOT NULL UNIQUE,type TEXT NOT NULL DEFAULT 'fixed',value INTEGER NOT NULL DEFAULT 0,min_spend INTEGER NOT NULL DEFAULT 0,starts_at TEXT,ends_at TEXT,usage_limit INTEGER,usage_count INTEGER NOT NULL DEFAULT 0,active INTEGER NOT NULL DEFAULT 1,created_at TEXT NOT NULL,updated_at TEXT NOT NULL,title TEXT,product_id TEXT,variant TEXT,promo_price_minor INTEGER,slug TEXT)"),e.DB.prepare("CREATE TABLE IF NOT EXISTS shop_settings(key TEXT PRIMARY KEY,value TEXT,updated_at TEXT NOT NULL)"),e.DB.prepare("CREATE TABLE IF NOT EXISTS order_addresses(order_id TEXT PRIMARY KEY REFERENCES orders(id) ON DELETE CASCADE,name TEXT,phone TEXT,address TEXT,shipping_method_id TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL)")]);}
async function evt(e,oid,type,meta={}){return e.DB.prepare('INSERT INTO activity_events(id,order_id,event_type,trace_id,metadata_json,created_at) VALUES(?,?,?,?,?,?)').bind(ID('evt'),oid,type,oid,JSON.stringify(meta),NOW()).run()}
export async function onRequest({request,env}){if(request.method==='OPTIONS')return new Response(null,{status:204,headers:C});if(!env.DB)return J({ok:false,error:'D1 binding DB tidak dijumpai'},503);try{const d=await body(request);if(!auth(request,env,d))return J({ok:false,error:'Unauthorized'},401);await ensure(env);switch(S(d.action)){case'listProducts':return listProducts(env);case'saveProduct':return saveProduct(d,env);case'deleteProduct':return hideProduct(d,env);case'listOrders':return listOrders(d,env);case'getOrder':return getOrder(d,env);case'listCustomers':return customers(env);case'getShipping':return shipping(env);case'saveShipping':return saveShipping(d,env);case'listPromotions':return promotions(env);case'savePromotion':return savePromotion(d,env);case'deletePromotion':return deletePromotion(d,env);case'paymentConfig':return paymentConfig(env);case'savePaymentConfig':return savePaymentConfig(d,env);case'status':return setStatus(d,env);case'receipt':return receipt(d,env);default:return J({ok:false,error:'Action tidak dikenali'},400)}}catch(e){console.error(e);return J({ok:false,error:e?.message||String(e)},500)}}
async function listProducts(e){const p=(await e.DB.prepare('SELECT * FROM products ORDER BY created_at DESC').all()).results||[],v=(await e.DB.prepare('SELECT * FROM product_variations ORDER BY created_at').all()).results||[],im=(await e.DB.prepare('SELECT * FROM product_images ORDER BY is_cover DESC,sort_order,id').all()).results||[];return J({ok:true,products:p.map(x=>{const image=im.find(y=>y.product_id===x.id)?.url||'';return{id:x.id,sku:x.sku||'',name:x.name,category:x.category||x.product_type,productType:x.product_type,description:x.description||'',desc:x.short_description||x.description||'',image,imageUrl:image,basePrice:Number(x.base_price_minor||0)/100,active:x.status==='active',status:x.status,variants:v.filter(y=>y.product_id===x.id).map(y=>({id:y.id,name:y.name,sku:y.sku||'',price:Number((y.sale_price_minor??y.price_minor)||0)/100,priceMinor:Number(y.price_minor||0),salePrice:y.sale_price_minor==null?null:Number(y.sale_price_minor)/100,stock:y.stock_qty,active:y.status==='active',image:y.image_url||''}))}})});}
async function saveProduct(d,e){const pid=S(d.id)||ID('prd'),t=NOW(),old=await e.DB.prepare('SELECT * FROM products WHERE id=?').bind(pid).first(),name=S(d.name);if(!name)return J({ok:false,error:'Nama produk diperlukan'},400);const type=S(d.productType||old?.product_type||'physical'),ful=S(d.fulfillmentType||old?.fulfillment_type||'physical_shipping'),status=d.active===false?'hidden':S(d.status||old?.status||'active'),base=d.basePriceMinor!=null?Number(d.basePriceMinor):Math.round(Number(d.basePrice||0)*100),category=S(d.category||old?.category||'');if(old)await e.DB.prepare('UPDATE products SET sku=?,name=?,slug=?,product_type=?,fulfillment_type=?,description=?,short_description=?,base_price_minor=?,sale_price_minor=?,currency=?,status=?,category=?,updated_at=? WHERE id=?').bind(S(d.sku||old.sku)||null,name,S(d.slug??old.slug)||null,type,ful,S(d.description??old.description)||null,S(d.shortDescription??old.short_description)||null,base,d.salePriceMinor==null?(old?.sale_price_minor??null):Number(d.salePriceMinor),S(d.currency||old.currency||'MYR'),status,category,t,pid).run();else await e.DB.prepare('INSERT INTO products(id,sku,name,slug,product_type,fulfillment_type,description,short_description,base_price_minor,sale_price_minor,currency,status,category,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)').bind(pid,S(d.sku)||null,name,S(d.slug)||null,type,ful,S(d.description),S(d.shortDescription||d.desc),base,d.salePriceMinor==null?null:Number(d.salePriceMinor),S(d.currency||'MYR'),status,category,t,t).run();if(Array.isArray(d.variants))await syncVariants(d,e,pid,t);if(d.imageUrl||d.image){await e.DB.prepare('DELETE FROM product_images WHERE product_id=?').bind(pid).run();await e.DB.prepare('INSERT INTO product_images(id,product_id,url,alt_text,sort_order,is_cover,created_at) VALUES(?,?,?,?,?,?,?)').bind(ID('img'),pid,S(d.imageUrl||d.image),name,0,1,t).run();}return J({ok:true,productId:pid});}
async function syncVariants(d,e,pid,t){const existing=(await e.DB.prepare('SELECT id,name FROM product_variations WHERE product_id=?').bind(pid).all()).results||[],byId=new Map(existing.map(x=>[x.id,x])),byName=new Map(existing.map(x=>[S(x.name).toLowerCase(),x])),seen=new Set();for(const raw of d.variants){const x=Array.isArray(raw)?{name:raw[0],price:raw[1]}:raw||{},name=S(x.name);if(!name)continue;const match=(x.id&&byId.get(S(x.id)))||byName.get(name.toLowerCase()),pm=x.priceMinor!=null?Number(x.priceMinor):Math.round(Number(x.price||0)*100),sm=x.salePriceMinor==null?(x.salePrice==null?null:Math.round(Number(x.salePrice)*100)):Number(x.salePriceMinor),stock=x.stock==null||x.stock===''?null:Number(x.stock),img=S(x.imageUrl||x.image);if(match){seen.add(match.id);await e.DB.prepare('UPDATE product_variations SET sku=?,name=?,attributes_json=?,price_minor=?,sale_price_minor=?,stock_qty=?,stock_tracking=?,image_url=?,status=?,updated_at=? WHERE id=? AND product_id=?').bind(S(x.sku)||null,name,JSON.stringify(x.attributes||{}),pm,sm,stock,x.stockTracking?1:0,img||null,x.active===false?'hidden':'active',t,match.id,pid).run()}else{const vid=ID('var');seen.add(vid);await e.DB.prepare('INSERT INTO product_variations(id,product_id,sku,name,attributes_json,price_minor,sale_price_minor,stock_qty,stock_tracking,image_url,status,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)').bind(vid,pid,S(x.sku)||null,name,JSON.stringify(x.attributes||{}),pm,sm,stock,x.stockTracking?1:0,img||null,x.active===false?'hidden':'active',t,t).run();}}for(const x of existing)if(!seen.has(x.id))await e.DB.prepare("UPDATE product_variations SET status='hidden',updated_at=? WHERE id=? AND product_id=?").bind(t,x.id,pid).run();}
async function hideProduct(d,e){const id=S(d.id);if(!id)return J({ok:false,error:'Product id diperlukan'},400);await e.DB.prepare("UPDATE products SET status='hidden',updated_at=? WHERE id=?").bind(NOW(),id).run();return J({ok:true})}
const orderNo=o=>S(o.order_no)||`RQ-${String(o.id).replace(/[^A-Za-z0-9]/g,'').slice(-12).toUpperCase()}`;
async function listOrders(d,e){const lim=Math.min(500,Math.max(1,Number(d.limit||100))),r=(await e.DB.prepare('SELECT o.*,c.name customer_name,c.phone,c.email FROM orders o LEFT JOIN customers c ON c.id=o.customer_id ORDER BY o.created_at DESC LIMIT ?').bind(lim).all()).results||[];return J({ok:true,orders:r.map(o=>({...o,orderNo:orderNo(o),order_ref:orderNo(o),name:o.customer_name||'',total:Number(o.total_minor||0)/100,status:o.fulfillment_status,payment:o.payment_status,timestamp:o.created_at}))});}
async function getOrder(d,e){const key=S(d.orderId||d.orderRef||d.orderNo),o=await e.DB.prepare('SELECT o.*,c.name customer_name,c.phone,c.email FROM orders o LEFT JOIN customers c ON c.id=o.customer_id WHERE o.id=? OR o.order_no=? LIMIT 1').bind(key,key).first();if(!o)return J({ok:false,error:'Order tidak dijumpai'},404);const it=(await e.DB.prepare('SELECT * FROM order_items WHERE order_id=? ORDER BY created_at').bind(o.id).all()).results||[],pay=(await e.DB.prepare('SELECT * FROM payments WHERE order_id=? ORDER BY created_at DESC').bind(o.id).all()).results||[],addr=await e.DB.prepare('SELECT * FROM order_addresses WHERE order_id=?').bind(o.id).first();return J({ok:true,order:{...o,orderNo:orderNo(o),name:o.customer_name||'',total:Number(o.total_minor||0)/100,subtotal:Number(o.subtotal_minor||0)/100,shipping:Number(o.shipping_minor||0)/100,discount:Number(o.discount_minor||0)/100,address:addr||null,payments:pay},items:it.map(x=>{let v={};try{v=JSON.parse(x.variation_snapshot_json||'{}')}catch{}return{...x,product_name:x.product_name_snapshot,qty:x.quantity,unit_price:Number(x.unit_price_minor||0)/100,line_total:Number(x.line_total_minor||0)/100,variant_name:v.name||''}})});}
async function customers(e){return J({ok:true,customers:(await e.DB.prepare('SELECT * FROM customers ORDER BY created_at DESC LIMIT 500').all()).results||[]})}
async function shipping(e){return J({ok:true,shipping:((await e.DB.prepare('SELECT id,name,price_minor,active,sort_order FROM shipping_methods ORDER BY sort_order,name').all()).results||[]).map(x=>({...x,price:Number(x.price_minor||0)/100,active:!!x.active}))});}
async function saveShipping(d,e){const sid=S(d.id)||ID('ship'),t=NOW(),name=S(d.name);if(!name)return J({ok:false,error:'Nama shipping diperlukan'},400);const p=d.priceMinor!=null?Number(d.priceMinor):Math.round(Number(d.price||0)*100),ex=await e.DB.prepare('SELECT id FROM shipping_methods WHERE id=?').bind(sid).first();if(ex)await e.DB.prepare('UPDATE shipping_methods SET name=?,price_minor=?,active=?,sort_order=?,updated_at=? WHERE id=?').bind(name,p,d.active===false?0:1,Number(d.sortOrder||0),t,sid).run();else await e.DB.prepare('INSERT INTO shipping_methods(id,name,price_minor,active,sort_order,created_at,updated_at) VALUES(?,?,?,?,?,?,?)').bind(sid,name,p,d.active===false?0:1,Number(d.sortOrder||0),t,t).run();return J({ok:true,id:sid});}
async function promotions(e){return J({ok:true,promotions:((await e.DB.prepare('SELECT pr.*,p.name product_name FROM promotions pr LEFT JOIN products p ON p.id=pr.product_id ORDER BY pr.created_at DESC').all()).results||[]).map(x=>({...x,title:x.title||x.code,slug:x.slug||String(x.code).toLowerCase(),productName:x.product_name||'',promoPrice:Number(x.promo_price_minor||0)/100,active:!!x.active,start:x.starts_at||'',end:x.ends_at||''}))});}
async function savePromotion(d,e){const id=S(d.id)||ID('promo'),code=S(d.code||d.slug).toUpperCase();if(!code)return J({ok:false,error:'Kod promo diperlukan'},400);const t=NOW(),pp=d.promoPriceMinor!=null?Number(d.promoPriceMinor):Math.round(Number(d.promoPrice||0)*100),orig=Math.round(Number(d.originalPrice||0)*100),value=Math.max(0,orig-pp),ex=await e.DB.prepare('SELECT id FROM promotions WHERE id=?').bind(id).first();if(ex)await e.DB.prepare('UPDATE promotions SET code=?,value=?,starts_at=?,ends_at=?,active=?,updated_at=?,title=?,product_id=?,variant=?,promo_price_minor=?,slug=? WHERE id=?').bind(code,value,d.start||null,d.end||null,d.active===false?0:1,t,S(d.title)||code,S(d.productId)||null,S(d.variant)||null,pp,code.toLowerCase(),id).run();else await e.DB.prepare('INSERT INTO promotions(id,code,type,value,min_spend,starts_at,ends_at,usage_limit,usage_count,active,created_at,updated_at,title,product_id,variant,promo_price_minor,slug) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)').bind(id,code,'fixed',value,0,d.start||null,d.end||null,null,0,d.active===false?0:1,t,t,S(d.title)||code,S(d.productId)||null,S(d.variant)||null,pp,code.toLowerCase()).run();return J({ok:true,id});}
async function deletePromotion(d,e){await e.DB.prepare('UPDATE promotions SET active=0,updated_at=? WHERE id=?').bind(NOW(),S(d.id)).run();return J({ok:true});}
async function paymentConfig(e){const r=(await e.DB.prepare("SELECT key,value FROM shop_settings WHERE key IN ('payment_qr_url','payment_account_name','payment_instructions')").all()).results||[];const x=Object.fromEntries(r.map(a=>[a.key,a.value]));return J({ok:true,qrUrl:x.payment_qr_url||'',accountName:x.payment_account_name||'',instructions:x.payment_instructions||''});}
async function savePaymentConfig(d,e){const t=NOW();for(const [k,v] of [['payment_qr_url',d.qrUrl],['payment_account_name',d.accountName],['payment_instructions',d.instructions]])if(v!==undefined)await e.DB.prepare('INSERT OR REPLACE INTO shop_settings(key,value,updated_at) VALUES(?,?,?)').bind(k,S(v),t).run();return paymentConfig(e);}
async function setStatus(d,e){const key=S(d.orderId||d.orderRef||d.orderNo),st=S(d.status).toLowerCase(),o=await e.DB.prepare('SELECT id FROM orders WHERE id=? OR order_no=? LIMIT 1').bind(key,key).first();if(!o)return J({ok:false,error:'Order tidak dijumpai'},404);if(['paid','failed','cancelled','refunded','pending','partial'].includes(st))await e.DB.prepare('UPDATE orders SET payment_status=?,updated_at=? WHERE id=?').bind(st,NOW(),o.id).run();else if(['processing','fulfilled','cancelled','pending'].includes(st))await e.DB.prepare('UPDATE orders SET fulfillment_status=?,updated_at=? WHERE id=?').bind(st,NOW(),o.id).run();else return J({ok:false,error:'Status tidak sah'},400);return J({ok:true,status:st});}
async function receipt(d,e){const r=await getOrder(d,e),j=await r.json();if(!j.ok)return J(j,404);const o=j.order,rows=(j.items||[]).map(x=>`<tr><td>${esc(x.product_name)}${x.variant_name?`<br><small>${esc(x.variant_name)}</small>`:''}</td><td>${x.qty}</td><td>RM${x.unit_price.toFixed(2)}</td><td>RM${x.line_total.toFixed(2)}</td></tr>`).join('');return new Response(`<!doctype html><html lang="ms"><head><meta charset="utf-8"><title>REQOO Receipt ${esc(o.orderNo)}</title><style>body{font-family:Arial;margin:40px;color:#111;max-width:800px;margin-inline:auto}table{width:100%;border-collapse:collapse;margin-top:25px}th,td{padding:10px;border-bottom:1px solid #ddd;text-align:left}td:nth-child(n+2),th:nth-child(n+2){text-align:right}.total{text-align:right;font-size:20px;font-weight:bold}</style></head><body><h1>REQOO.CO</h1><p>Receipt / Resit<br>No: ${esc(o.orderNo)}<br>Tarikh: ${esc(o.created_at)}<br>Status: ${esc(o.payment_status)}</p><p><b>Customer</b><br>${esc(o.name)}<br>${esc(o.phone)}<br>${esc(o.email)}</p><table><thead><tr><th>Item</th><th>Qty</th><th>Harga</th><th>Jumlah</th></tr></thead><tbody>${rows}</tbody></table><p>Subtotal: RM${o.subtotal.toFixed(2)}<br>Shipping: RM${o.shipping.toFixed(2)}<br>Discount: RM${o.discount.toFixed(2)}</p><div class="total">TOTAL RM${o.total.toFixed(2)}</div><script>window.print()</script></body></html>`,{headers:{'content-type':'text/html;charset=UTF-8',...C}})}
function esc(x){return String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
