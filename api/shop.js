const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"Content-Type, X-Admin-Key, X-Admin-Token","Access-Control-Allow-Methods":"GET,POST,OPTIONS","Cache-Control":"no-store"};
const json=(d,s=200)=>new Response(JSON.stringify(d),{status:s,headers:{...cors,"Content-Type":"application/json; charset=UTF-8"}});
const text=v=>String(v??'').trim();
const id=p=>`${p}_${crypto.randomUUID().replaceAll('-','')}`;
const money=v=>{const n=Number(v);return Number.isFinite(n)&&n>=0?Math.round(n):0};
const esc=v=>text(v).slice(0,5000);
function admin(request,env){const expected=text(env.REQOO_ADMIN_TOKEN||env.ADMIN_KEY),supplied=text(request.headers.get('X-Admin-Token')||request.headers.get('X-Admin-Key'));return !!expected&&expected===supplied}
function productPrice(p,v){return money(v?.sale_price_minor??v?.price_minor??p.sale_price_minor??p.base_price_minor)}
async function products(env){const rows=await env.DB.prepare("SELECT * FROM products WHERE status='active' ORDER BY name ASC").all();const out=[];for(const p of rows.results||[]){const [imgs,vars]=await Promise.all([env.DB.prepare("SELECT url,alt_text,is_cover,sort_order FROM product_images WHERE product_id=? ORDER BY is_cover DESC,sort_order ASC").bind(p.id).all(),env.DB.prepare("SELECT id,name,price_minor,sale_price_minor,status FROM product_variations WHERE product_id=? AND status='active' ORDER BY sort_order,name").bind(p.id).all()]);const image=(imgs.results||[])[0]?.url||'';out.push({id:p.id,name:p.name,category:p.product_type==='service'?'Service':(p.fulfillment_type==='physical_shipping'?'Custom':p.product_type),desc:p.short_description||p.description||'',image,active:true,sortOrder:0,variants:(vars.results||[]).map(v=>[v.name,productPrice(p,v),v.price_minor??p.base_price_minor])});}return out}
async function findOrCreateCustomer(env,name,phone){const old=await env.DB.prepare("SELECT * FROM customers WHERE phone=? LIMIT 1").bind(phone).first();if(old)return old;const at=new Date().toISOString(),cid=id('cus');await env.DB.prepare("INSERT INTO customers(id,name,phone,email,status,created_at,updated_at) VALUES(?,?,?,?,?,?,?)").bind(cid,name,phone,null,'active',at,at).run();return await env.DB.prepare("SELECT * FROM customers WHERE id=?").bind(cid).first()}
async function billplz(d,env){const key=text(env.BILLPLZ_API_KEY||env.BILLPLZ_SECRET_KEY),collection=text(env.BILLPLZ_COLLECTION_ID||env.BILLPLZ_COLLECTION||env.BILLPLZ_COLLECT);if(!key||!collection)return null;const base=text(env.BILLPLZ_ENV).toLowerCase()==='production'?'https://www.billplz.com/api':'https://www.billplz-sandbox.com/api';const origin=text(env.REQOO_PUBLIC_ORIGIN||'https://reqoo.co').replace(/\/$/,'');const form=new URLSearchParams({collection_id:collection,description:`REQOO Order ${d.orderNo}`,email:d.email,name:d.name,mobile:d.phone,amount:String(d.amount_minor),callback_url:`${origin}/api/billplz`,redirect_url:`${origin}/shop/?payment=returned&order=${encodeURIComponent(d.orderNo)}`});const r=await fetch(`${base}/v4/bills`,{method:'POST',headers:{Authorization:'Basic '+btoa(key+':'),'Content-Type':'application/x-www-form-urlencoded'},body:form});const j=await r.json().catch(()=>({}));if(!r.ok)return {error:j?.error?.message||j?.error||`Billplz HTTP ${r.status}`};return {billId:j.id,billUrl:j.url,state:j.state||'due'} }
async function createOrder(request,env){let d;try{d=await request.json()}catch{return json({ok:false,error:'Payload order tidak sah.'},400)}if(!env.DB)return json({ok:false,error:'D1 binding DB tidak dijumpai.'},503);const name=text(d.name),phone=text(d.phone),address=esc(d.address);if(!name||!phone)return json({ok:false,error:'Nama dan No. WhatsApp diperlukan.'},400);const rawItems=Array.isArray(d.items)?d.items:[];if(!rawItems.length)return json({ok:false,error:'Cart kosong.'},400);const all=await products(env),map=new Map(all.map(p=>[String(p.id),p])),items=[];let subtotal=0;for(const raw of rawItems){const p=map.get(text(raw.productId));if(!p)return json({ok:false,error:`Produk tidak dijumpai: ${text(raw.productId)}`},400);const vars=Array.isArray(p.variants)?p.variants:[];const v=vars.find(x=>String(x[0])===text(raw.variant))||vars[0];const qty=Math.min(99,Math.max(1,Math.round(Number(raw.qty)||1)));const unit=money(v?.[1]);if(unit<=0)return json({ok:false,error:`Produk ${p.name} memerlukan quotation.`},400);const line=unit*qty;subtotal+=line;items.push({p,variant:v,qty,unit,line,raw})}const customer=await findOrCreateCustomer(env,name,phone);const now=new Date().toISOString(),orderId=id('ord_'),orderNo=`RQ-${now.replace(/[-:TZ.]/g,'').slice(0,14)}-${crypto.randomUUID().slice(0,5).toUpperCase()}`;const stmts=[env.DB.prepare("INSERT INTO orders(id,customer_id,source,currency,subtotal_minor,discount_minor,shipping_minor,tax_minor,total_minor,payment_status,fulfillment_status,referral_code,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?)").bind(orderId,customer.id,'shop','MYR',subtotal,0,0,0,subtotal,'pending','pending',null,now,now)];for(const x of items)stmts.push(env.DB.prepare("INSERT INTO order_items(id,order_id,product_id,variation_id,product_name_snapshot,sku_snapshot,variation_snapshot_json,customization_snapshot_json,addons_snapshot_json,unit_price_minor,quantity,line_total_minor,created_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)").bind(id('itm_'),orderId,x.p.id,null,x.p.name,null,JSON.stringify({name:x.variant?.[0]||''}),JSON.stringify({name:text(x.raw.customText),note:text(x.raw.note),address}),JSON.stringify([]),x.unit,x.qty,x.line,now));stmts.push(env.DB.prepare("INSERT INTO activity_events(id,customer_id,order_id,event_type,trace_id,metadata_json,created_at) VALUES(?,?,?,?,?,?,?)").bind(id('evt_'),customer.id,orderId,'order.created',null,JSON.stringify({source:'shop',order_no:orderNo,address}),now));await env.DB.batch(stmts);let payment=null;if(subtotal>0){const email=text(d.email)||`order-${orderId.slice(-12)}@reqoo.co`;payment=await billplz({orderNo,name,email,phone,amount_minor:subtotal},env);if(payment?.error)return json({ok:false,error:payment.error,order_id:orderId,order_no:orderNo},502);if(payment?.billId){await env.DB.prepare("INSERT INTO payments(id,order_id,provider,provider_reference,method,amount_minor,currency,status,metadata_json,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?)").bind(id('pay_'),orderId,'billplz',payment.billId,'billplz',subtotal,'MYR','pending',JSON.stringify(payment),now,now).run()}}
return json({ok:true,id:orderId,order_id:orderId,order_no:orderNo,total:subtotal/100,total_minor:subtotal,items:items.map(x=>({name:x.p.name,variant:x.variant?.[0]||'',qty:x.qty,unitPrice:x.unit/100})),payment:payment?.billUrl?{billUrl:payment.billUrl,billId:payment.billId}:null},201)}
async function callback(request,env){let d={};try{if((request.headers.get('content-type')||'').includes('application/json'))d=await request.json();else d=Object.fromEntries(new URLSearchParams(await request.text()))}catch{}const sig=text(d.x_signature),secret=text(env.BILLPLZ_X_SIGNATURE_KEY);if(!sig||!secret)return new Response('invalid', {status:400});const pairs=Object.entries(d).filter(([k,v])=>k!=='x_signature'&&v!==undefined&&v!==null).map(([k,v])=>[k,String(v)]).sort((a,b)=>a[0].toLowerCase().localeCompare(b[0].toLowerCase()));const source=pairs.map(([k,v])=>k+v).join('|');const key=await crypto.subtle.importKey('raw',new TextEncoder().encode(secret),{name:'HMAC',hash:'SHA-256'},false,['sign']);const digest=await crypto.subtle.sign('HMAC',key,new TextEncoder().encode(source));const computed=[...new Uint8Array(digest)].map(x=>x.toString(16).padStart(2,'0')).join('');if(computed.toLowerCase()!==sig.toLowerCase())return new Response('invalid signature',{status:400});const billId=text(d.id);const paid=text(d.paid).toLowerCase()==='true';const state=text(d.state).toLowerCase();const payment=await env.DB.prepare("SELECT * FROM payments WHERE provider='billplz' AND provider_reference=? LIMIT 1").bind(billId).first();if(payment){const status=paid?'paid':(state==='cancelled'?'cancelled':'pending');await env.DB.prepare("UPDATE payments SET status=?,paid_at=?,metadata_json=?,updated_at=? WHERE id=?").bind(status,paid?new Date().toISOString():null,JSON.stringify(d),new Date().toISOString(),payment.id).run();await env.DB.prepare("UPDATE orders SET payment_status=?,updated_at=? WHERE id=?").bind(status,new Date().toISOString(),payment.order_id).run()}return new Response('OK',{status:200,headers:cors})}
export async function handle(request,env){if(request.method==='OPTIONS')return new Response(null,{status:204,headers:cors});const u=new URL(request.url);if(u.pathname==='/api/billplz')return callback(request,env);if(u.pathname==='/api/shop'&&request.method==='GET'){const a=text(u.searchParams.get('action'));if(a==='listProducts')return json({ok:true,products:await products(env)});if(a==='getShipping')return json({ok:true,shipping:[]});if(a==='getPromotion')return json({ok:false,error:'Promotion tidak dikonfigurasi'},404);return json({ok:false,error:'Action tidak dikenali'},400)}if(u.pathname==='/api/shop'&&request.method==='POST')return createOrder(request,env);return json({ok:false,error:'Not found'},404)}