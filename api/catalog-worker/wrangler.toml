name = "reqoo-api"
main = "src/index.js"
compatibility_date = "2026-08-19"

[[d1_databases]]
binding = "DB"
database_name = "reqoo-rebuild"
# database_id is intentionally added only after the exact Cloudflare D1 UUID is confirmed.
# Do not substitute the old `reqoo` database.

[vars]
# Set ADMIN_KEY as a Cloudflare secret; never commit the real value.
