const JSON_HEADERS = { 'Content-Type': 'application/json; charset=utf-8' };
const ORIGINS = new Set(['https://admin.reqoo.co','https://shop.reqoo.co','https://reqoo.co','https://www.reqoo.co']);
const json = (data, status, origin) => { const h = new Headers(JSON_HEADERS); h.set('Access-Control-Allow-Origin', ORIGINS.has(origin) ? origin : 'null'); h.set('Access-Control-Allow-Credentials', 'true'); h.set('Access-Control-Allow-Methods', 'POST,OPTIONS'); h.set('Access-Control-Allow-Headers', 'Content-Type, Idempotency-Key'); h.set('Vary', 'Origin'); h.set('Cache-Control', 'no-store'); return new Response(JSON.stringify(data), { status, headers: h }); };
function meta(raw) { const prefix='__REQOO_PRODUCT_V2__:',text=String(raw||'');if(!text.startsWith(prefix))return{options:[]};try{const bin=atob(text.slice(prefix.length)),decoded=decodeURIComponent(escape(bin)),value=JSON.parse(decoded);return{options:Array.isArray(value?.options)?value.options:[]}}catch{return{options:[]}} }
function cleanText(v,max=1000){return String(v??'').trim().slice(0,max)}function chosen(v){return v&&typeof v==='object'?String(v.value??v.label??''):String(v??'')}function moneyCents(v){const n=Number(v);return Number.isFinite(n)&&n>=0?Math.round(n*100):NaN}function selectedAddon(addons,opt){return addons.find(a=>String(a?.option_id??a?.id??'')===String(opt.id)||String(a?.label??'')===String(opt.label))}
function priceProduct(product,item){const options=meta(product.internal_notes).options||[],variation=item.variation&&typeof item.variation==='object'?item.variation:{},customization=item.customization&&typeof item.customization==='object'?item.customization:{},addons=Array.isArray(item.addons)?item.addons:[];let unit=Number(product.sale_price_minor??product.base_price_minor??0);if(!Number.isInteger(unit)||unit<0)throw new Error(`Harga product tidak sah: ${product.id}`);
for(const opt of options.filter(x=>x.group==='variation')){const value=chosen(variation[opt.id]??variation[opt.label]);if(opt.required&&!value)throw new Error(`Pilihan ${opt.label} diperlukan`);if(!value)continue;const match=(opt.values||[]).find(v=>String(v.value??v.label)===value);if(!match)throw new Error(`Pilihan variation tidak sah: ${opt.label}`);const cents=moneyCents(match.price);if(!Number.isInteger(cents))throw new Error(`Harga variation tidak sah: ${opt.label}`);unit+=cents}
for(const opt of options.filter(x=>x.group==='addon')){const selected=selectedAddon(addons,opt);if(!selected){if(opt.required)throw new Error(`Add-on ${opt.label} diperlukan`);continue}let values=selected.values??selected.value??selected.label??true;if(!Array.isArray(values))values=[values];if(!values.length&&opt.required)throw new Error(`Add-on ${opt.label} diperlukan`);for(const raw of values){const label=raw&&typeof raw==='object'?String(raw.label??raw.value??''):String(raw);if(opt.field_type==='select'){const match=(opt.values||[]).find(v=>String(v.label)===label||String(v.value??'')===label);if(!match)throw new Error(`Pilihan add-on tidak sah: ${opt.label}`);const cents=moneyCents(match.price);if(!Number.isInteger(cents))throw new Error(`Harga add-on tidak sah: ${opt.label}`);unit+=cents}else{const match=(opt.values||[]).find(v=>String(v.label)===label||String(v.value??'')===label);if((opt.values||[]).length&&!match)throw new Error(`Pilihan add-on tidak sah: ${opt.label}`);const cents=match&&match.price!==undefined?moneyCents(match.price):moneyCents(opt.addonPrice||0);if(!Number.isInteger(cents))throw new Error(`Harga add-on tidak sah: ${opt.label}`);unit+=cents}}}
for(const opt of options.filter(x=>x.group==='custom')){const raw=customization[opt.id]??customization[opt.label]??'',value=raw&&typeof raw==='object'?raw.value??'':raw;if(opt.required&&!String(value).trim())throw new Error(`Ruangan ${opt.label} diperlukan`)}return{unit,variation,customization,addons}}
export async function createOrder(request,env){const origin=request.headers.get('Origin')||'';if(request.method==='OPTIONS')return json({},204,origin);if(request.method!=='POST')return json({error:'Method not allowed'},405,origin);if(!env.DB)return json({error:'D1 binding DB is not configured'},500,origin);const idempotencyKey=cleanText(request.headers.get('Idempotency-Key'),200);if(idempotencyKey.length<16)return json({error:'Idempotency-Key diperlukan'},400,origin);let input;try{input=await request.json()}catch{return json({error:'Data pesanan tidak sah'},400,origin)}const name=cleanText(input?.name,120),phone=cleanText(input?.phone,40),email=cleanText(input?.email,160).toLowerCase(),shippingAddress=cleanText(input?.shipping_address,1000),orderNote=cleanText(input?.order_note,1000);if(!name||!phone||!shippingAddress)return json({error:'Nama, telefon dan alamat diperlukan'},400,origin);if(!Array.isArray(input?.items)||!input.items.length)return json({error:'Cart kosong'},400,origin);if(input.items.length>50)return json({error:'Cart terlalu banyak item'},400,origin);try{const existing=await env.DB.prepare('SELECT id,public_token,total_minor,payment_status,fulfillment_status FROM orders WHERE idempotency_key=? LIMIT 1').bind(idempotencyKey).first();if(existing)return json({order:{id:existing.id,public_token:existing.public_token,total_minor:existing.total_minor,payment_status:existing.payment_status,fulfillment_status:existing.fulfillment_status},idempotent:true},200,origin)}catch{return json({error:'Checkout database migration 004 belum digunakan'},503,origin)}
const requested=input.items.map(x=>({productId:cleanText(x?.product_id||x?.id,100),quantity:Math.max(1,Math.min(999,Math.floor(Number(x?.quantity||1)))),customization:x?.customization&&typeof x.customization==='object'?x.customization:{},variation:x?.variation&&typeof x.variation==='object'?x.variation:{},addons:Array.isArray(x?.addons)?x.addons:[]})),ids=[...new Set(requested.map(x=>x.productId).filter(Boolean))];if(ids.length!==requested.length)return json({error:'Product ID tidak sah'},400,origin);const placeholders=ids.map(()=>'?').join(','),{results}=await env.DB.prepare(`SELECT id,sku,name,base_price_minor,sale_price_minor,status,internal_notes FROM products WHERE id IN (${placeholders})`).bind(...ids).all(),products=new Map((results||[]).map(p=>[p.id,p]));let subtotal=0;const lines=[];for(const item of requested){const product=products.get(item.productId);if(!product||product.status!=='active')return json({error:`Product tidak tersedia: ${item.productId}`},409,origin);const priced=priceProduct(product,item),line=priced.unit*item.quantity;if(!Number.isSafeInteger(line))return json({error:'Jumlah pesanan terlalu besar'},400,origin);subtotal+=line;if(!Number.isSafeInteger(subtotal))return json({error:'Jumlah pesanan terlalu besar'},400,origin);lines.push({product,...item,...priced,line})}
const customerId=crypto.randomUUID(),orderId=crypto.randomUUID(),publicToken=crypto.randomUUID().replaceAll('-','')+crypto.randomUUID().replaceAll('-',''),now=new Date().toISOString(),statements=[env.DB.prepare('INSERT INTO customers (id,name,phone,email,status,created_at,updated_at) VALUES (?,?,?,?,?,?,?)').bind(customerId,name,phone,email||null,'active',now,now),env.DB.prepare('INSERT INTO orders (id,customer_id,source,currency,subtotal_minor,discount_minor,shipping_minor,tax_minor,total_minor,payment_status,fulfillment_status,shipping_address,order_note,idempotency_key,public_token,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)').bind(orderId,customerId,'shop','MYR',subtotal,0,0,0,subtotal,'pending','pending',shippingAddress,orderNote||null,idempotencyKey,publicToken,now,now),...lines.map(x=>env.DB.prepare('INSERT INTO order_items (id,order_id,product_id,product_name_snapshot,sku_snapshot,variation_snapshot_json,customization_snapshot_json,addons_snapshot_json,unit_price_minor,quantity,line_total_minor,created_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?)').bind(crypto.randomUUID(),orderId,x.product.id,x.product.name,x.product.sku||null,JSON.stringify(x.variation),JSON.stringify(x.customization),JSON.stringify(x.addons),x.unit,x.quantity,x.line,now))];try{await env.DB.batch(statements)}catch(error){const winner=await env.DB.prepare('SELECT id,public_token,total_minor,payment_status,fulfillment_status FROM orders WHERE idempotency_key=? LIMIT 1').bind(idempotencyKey).first();if(winner)return json({order:{id:winner.id,public_token:winner.public_token,total_minor:winner.total_minor,payment_status:winner.payment_status,fulfillment_status:winner.fulfillment_status},idempotent:true},200,origin);console.error(error);return json({error:'Pesanan gagal disimpan'},500,origin)}return json({order:{id:orderId,public_token:publicToken,total_minor:subtotal,payment_status:'pending',fulfillment_status:'pending'},idempotent:false},201,origin)}
