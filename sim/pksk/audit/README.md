# PKSK isolated audit workspace

This branch is an audit clone of the live simulator source. Do not merge simulator set changes from this branch into `main`.

Audit results are used to build a separate curated master bank. The live `sim/pksk/simulator/sets` files are source evidence only and must remain unchanged during audit.
