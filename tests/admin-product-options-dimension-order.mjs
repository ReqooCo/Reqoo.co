import fs from 'node:fs';
import assert from 'node:assert/strict';
const js=fs.readFileSync(new URL('../admin/product-option-matrix-v1.js',import.meta.url),'utf8');
const html=fs.readFileSync(new URL('../admin/products.html',import.meta.url),'utf8');
assert.match(js,/looksLikeSize/,'existing two-dimensional variants must detect which dimension is size');
assert.match(js,/swapDimensions/,'existing color\/size variants must be normalized to size\/color in the editor');
assert.match(js,/Saiz & harga/,'primary option UI should be explicitly size and price');
assert.match(js,/Warna/,'secondary option UI should be explicitly color');
assert.doesNotMatch(js,/Pilihan 1/,'generic Pilihan 1 label should not remain in the simplified editor');
assert.doesNotMatch(js,/Pilihan 2/,'generic Pilihan 2 label should not remain in the simplified editor');
assert.match(html,/product-option-matrix-v1\.js\?v=4/,'hotfix must bump option editor asset version');
console.log('PASS: product option dimensions normalize to Size first and Color second.');
