Shop Admin is intentionally isolated from the universal Admin shell after the unified runtime caused browser unresponsiveness. Future Orders, Production, and Products work should use separate lightweight pages or a shared static visual shell, not inject the full admin-shell runtime into legacy shop/admin.html.