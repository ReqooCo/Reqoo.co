Stable fallback first. Separate pages second. Visual consistency will be achieved through shared static CSS/components rather than mounting the legacy Shop Admin page inside the premium Admin runtime.