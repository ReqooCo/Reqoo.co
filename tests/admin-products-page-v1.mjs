import fs from 'node:fs';import assert from 'node:assert/strict';const html=fs.readFileSync(new URL('../admin/products.html',import.meta.url),'utf8');const js=fs.readFileSync(new URL('../admin/products.js',import.meta.url),'utf8');const css=fs.readFileSync(new URL('../admin/products.css',import.meta.url),'utf8');const shell=fs.readFileSync(new URL('../admin/admin-shell.js',import.meta.url),'utf8');const worker=fs.readFileSync(new URL('../_web_worker.js',import.meta.url),'utf8');assert.match(html,/PRODUCT CONTROL/);assert.match(html,/LOW STOCK/);assert.match(html,/productForm/);assert.match(js,/productsDashboard/);assert.match(js,/saveProduct/);assert.match(js,/stock!==null/);assert.match(js,/value===''\?null/);assert.doesNotMatch(js,/MutationObserver/);assert.doesNotMatch(js,/visibilitychange/);assert.doesNotMatch(js,/setInterval/);assert.match(shell,/\/admin\/products\.html/);assert.match(worker,/production\|products/);assert.match(css,/\.rqProductDrawer/);console.log('PASS: standalone Products page stays lightweight and preserves untracked stock semantics.');