import fs from 'node:fs';
import assert from 'node:assert/strict';
const worker=fs.readFileSync(new URL('../_web_worker.js',import.meta.url),'utf8');
const css=fs.readFileSync(new URL('../admin/admin-base.css',import.meta.url),'utf8');
assert.match(worker,/admin-base\.css\?v=1/,'canonical Admin base must be injected');
assert.match(worker,/admin-ui-v1/,'Admin response must advertise canonical UI v1');
assert.match(css,/--rq-bg:#f7f7f5/,'workspace must use a neutral light background');
assert.match(css,/--rq-sidebar:#151515/,'navigation must use a restrained dark sidebar');
assert.match(css,/\.rqAdminSide/,'desktop sidebar must be themed');
assert.match(css,/\.rqAdminMobile/,'mobile dock must be themed');
assert.match(css,/\.modalBox/,'modal surfaces must be themed');
assert.match(css,/\.ordersTable/,'order data surfaces must be themed');
assert.match(css,/\.module-grid/,'overview modules must be themed');
assert.match(css,/@media\(max-width:900px\)/,'base theme must remain responsive');
console.log('PASS: canonical Admin base contains the shared premium theme and responsive shell.');
