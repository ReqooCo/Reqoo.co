import fs from 'node:fs';
import assert from 'node:assert/strict';
const html=fs.readFileSync(new URL('../admin/products.html',import.meta.url),'utf8');
const js=fs.readFileSync(new URL('../admin/product-option-matrix-v1.js',import.meta.url),'utf8');
const css=fs.readFileSync(new URL('../admin/products-v1.css',import.meta.url),'utf8');
assert.match(html,/product-option-matrix-v1\.js\?v=2/,'Products editor must load latest option helper');
assert.match(js,/Pilihan produk/,'helper must expose a simple options section');
assert.match(js,/Saiz & harga/,'size and price should be entered together');
assert.match(js,/Warna/,'helper must support shared colors');
assert.match(js,/Bina pilihan/,'helper must provide one clear build action');
assert.match(js,/sizes\.flatMap/,'helper must generate size x color combinations');
assert.match(js,/Butiran variasi \/ stok/,'generated variant details should be secondary/collapsible');
assert.match(css,/\.rqSimpleOptions/,'simple option builder must have dedicated styling');
console.log('PASS: simplified product size/color option builder present.');
