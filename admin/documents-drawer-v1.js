(()=>{
'use strict';
const API='/api/shop-admin';
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=n=>'RM'+(Number(n||0)/100).toFixed(2);
const dateFmt=v=>{if(!v)return'—';const d=new Date(v);return Number.isNaN(d.getTime())?String(v):d.toLocaleDateString('ms-MY',{day:'2-digit',month:'short',year:'numeric'})};
const methodLabel=v=>({bank_transfer:'Bank Transfer',cash:'Cash',qr:'QR / DuitNow',toyyibpay:'ToyyibPay',other:'Other'})[String(v||'').toLowerCase()]||String(v||'—');
const paymentType=v=>({deposit:'Deposit',partial:'Partial Payment',final:'Final Payment',full:'Full Payment'})[String(v||'').toLowerCase()]||String(v||'Payment');
const docLabel=v=>({quotation:'Quotation',invoice:'Invoice',receipt:'Official Receipt',delivery_order:'Delivery Order'})[v]||String(v||'Document');
const docIcon=v=>({quotation:'QT',invoice:'IN',delivery_order:'DO',receipt:'RC'})[v]||'DC';
let selectedNumber='',selectedDocument=null,selectedPayment=null,currentChain=[],currentPayments=[],currentSummary=null;
async function api(action,extra={}){const url=new URL(API,location.origin);url.searchParams.set('action',action);Object.entries(extra).forEach(([k,v])=>{if(v!==undefined&&v!==null&&v!=='')url.searchParams.set(k,v)});const r=await fetch(url,{cache:'no-store'});let d={};try{d=await r.json()}catch{}if(r.status===401){location.href='/admin/?return='+encodeURIComponent(location.pathname+location.search);throw Error('Sesi Admin tamat.')}if(!r.ok||d.ok===false)throw Error(d.error||'Request gagal');return d}
function ensureDrawer(){if($('#rqDocDrawer'))return;document.body.insertAdjacentHTML('beforeend',`<div class="rqDocDrawerBackdrop" id="rqDocDrawerBackdrop"></div><aside class="rqDocDrawer" id="rqDocDrawer" aria-hidden="true" aria-label="Document details"><div class="rqDocDrawerHead"><div><div class="rqDocDrawerEyebrow">DOCUMENT FLOW</div><h2 id="rqDocDrawerTitle">Document Details</h2><p id="rqDocDrawerSub">Quotation → Invoice → Payment → Receipt → Delivery Order</p></div><button class="rqDocDrawerClose" id="rqDocDrawerClose" type="button" aria-label="Tutup">×</button></div><div class="rqDocDrawerBody" id="rqDocDrawerBody"><div class="rqDocDrawerLoading">Pilih mana-mana dokumen untuk lihat aliran lengkap.</div></div></aside>`);$('#rqDocDrawerClose').onclick=closeDrawer;$('#rqDocDrawerBackdrop').onclick=closeDrawer;document.addEventListener('keydown',e=>{if(e.key==='Escape'&&$('#rqDocDrawer')?.classList.contains('open'))closeDrawer()})}
function closeDrawer(){const d=$('#rqDocDrawer'),b=$('#rqDocDrawerBackdrop');d?.classList.remove('open');b?.classList.remove('open');d?.setAttribute('aria-hidden','true');document.body.classList.remove('rqDocDrawerOpen')}
function showDrawer(){ensureDrawer();$('#rqDocDrawer').classList.add('open');$('#rqDocDrawerBackdrop').classList.add('open');$('#rqDocDrawer').setAttribute('aria-hidden','false');document.body.classList.add('rqDocDrawerOpen')}
function rowNumber(row){return row?.querySelector('.rqHistNo')?.textContent?.trim()||''}
function same(a,b){return String(a??'')===String(b??'')}
function docStatus(doc,summary){if(doc.type==='invoice'&&summary){const s=String(summary.paymentStatus||'pending').toLowerCase();return{label:s==='paid'?'PAID':s==='partial'?'PARTIAL':'UNPAID',cls:s==='paid'?'paid':s==='partial'?'partial':''}}const s=String(doc.status||doc.payment_status||'issued').toLowerCase();return{label:s.toUpperCase(),cls:s==='paid'||s==='accepted'?'paid':s==='partial'?'partial':''}}
function chainHtml(chain,summary){if(!chain.length)return'<div class="rqPaymentEmpty">Belum ada dokumen berkaitan.</div>';return `<div class="rqDocChain">${chain.map(d=>{const st=docStatus(d,summary),current=same(d.number,selectedNumber);return `<div class="rqDocChainItem${current?' current':''}"><div class="rqDocChainIcon">${esc(docIcon(d.type))}</div><div class="rqDocChainText"><b>${esc(docLabel(d.type))} · ${esc(d.number)}</b><span>${esc(dateFmt(d.issued_at||d.created_at))}${d.due_at?' · Due '+esc(dateFmt(d.due_at)):''}</span></div><div class="rqDocChainActions"><span class="rqDocMiniStatus ${st.cls}">${esc(st.label)}</span><button class="rqDocMiniBtn" type="button" data-drawer-open-doc-id="${esc(d.id)}">Open</button></div></div>`}).join('')}</div>`}
function paymentsHtml(list){if(!list.length)return'<div class="rqPaymentEmpty">Belum ada bayaran direkodkan untuk order ini.</div>';return `<div class="rqPaymentTimeline">${list.map(p=>`<div class="rqPaymentEvent"><div class="rqPaymentEventTop"><div><b>${esc(paymentType(p.payment_type))}</b><div class="rqPaymentEventMeta">${esc(dateFmt(p.paid_at))} · ${esc(methodLabel(p.method))}${p.reference?' · Ref '+esc(p.reference):''}</div></div><b class="rqPaymentEventAmount">${money(p.amount_minor)}</b></div><button class="rqPaymentReceiptLink" type="button" data-drawer-open-payment="${esc(p.id)}">${esc(p.receipt_number)} · Open receipt</button></div>`).join('')}</div>`}
function customerFrom(data){const source=selectedDocument||currentChain.find(Boolean)||{};return{name:source.customer_name||selectedPayment?.customer_name||data.order?.customer_name||'Customer',phone:source.customer_phone||selectedPayment?.customer_phone||data.order?.customer_phone||'',email:source.customer_email||selectedPayment?.customer_email||data.order?.customer_email||''}}
async function openByNumber(number){ensureDrawer();selectedNumber=number;showDrawer();$('#rqDocDrawerTitle').textContent=number||'Document Details';$('#rqDocDrawerBody').innerHTML='<div class="rqDocDrawerLoading">Memuatkan document flow…</div>';try{const d=await api('documentFlow',{key:number});selectedDocument=d.selectedDocument||null;selectedPayment=d.selectedPayment||null;currentChain=Array.isArray(d.documents)?d.documents:[];currentPayments=Array.isArray(d.payments)?d.payments:[];currentSummary=d.summary||null;const orderId=selectedDocument?.order_id||selectedPayment?.order_id||d.order?.id||'',invoice=currentChain.find(x=>x.type==='invoice'),quote=currentChain.find(x=>x.type==='quotation'),customer=customerFrom(d),fallbackTotal=Number(invoice?.total_minor??quote?.total_minor??selectedDocument?.total_minor??0),rawPaid=currentPayments.reduce((s,p)=>s+Number(p.amount_minor||0),0),total=Number(currentSummary?.totalMinor??fallbackTotal),paid=Number(currentSummary?.paidMinor??Math.min(total,rawPaid)),balance=Number(currentSummary?.balanceMinor??Math.max(0,total-rawPaid)),overpaid=Number(currentSummary?.overpaidMinor??Math.max(0,rawPaid-total)),orderNo=currentSummary?.orderNo||selectedPayment?.order_no||d.order?.order_no||(!String(orderId).startsWith('custom:')?orderId:'Custom quotation');$('#rqDocDrawerSub').textContent=`${customer.name} · ${orderNo}`;$('#rqDocDrawerBody').innerHTML=`<div class="rqDocDrawerCustomer"><div class="rqDocDrawerCustomerTop"><div><h3>${esc(customer.name)}</h3><p>${esc([customer.phone,customer.email].filter(Boolean).join(' · ')||'Tiada contact disimpan')}</p></div><span class="rqDocDrawerOrder">${esc(orderNo)}</span></div></div><div class="rqDocDrawerMoney"><div><span>TOTAL</span><b>${money(total)}</b></div><div class="paid"><span>PAID</span><b>${money(paid)}</b></div><div class="balance"><span>BALANCE</span><b>${money(balance)}</b></div></div>${overpaid>0?`<div class="rqDocDrawerAlert">Historical overpayment detected: ${money(overpaid)}. Payment baru telah disekat sehingga data disemak.</div>`:''}<section class="rqDocDrawerSection"><div class="rqDocDrawerSectionHead"><h4>Document Flow</h4><span class="rqDocDrawerCount">${currentChain.length} document${currentChain.length===1?'':'s'}</span></div>${chainHtml(currentChain,currentSummary)}</section><section class="rqDocDrawerSection"><div class="rqDocDrawerSectionHead"><h4>Payment Timeline</h4><span class="rqDocDrawerCount">${currentPayments.length} payment${currentPayments.length===1?'':'s'}</span></div>${paymentsHtml(currentPayments)}</section><div class="rqDocDrawerFooter"><button class="btn" id="rqDrawerOpenSelected" type="button">Open Selected</button>${invoice&&balance>0?`<button class="btn primary" id="rqDrawerRecordPayment" type="button">Record Payment · ${money(balance)}</button>`:''}</div>`;$$('[data-drawer-open-doc-id]').forEach(b=>b.onclick=e=>{e.stopPropagation();closeDrawer();document.dispatchEvent(new CustomEvent('rq:documents-open-id',{detail:{id:b.dataset.drawerOpenDocId}}))});$$('[data-drawer-open-payment]').forEach(b=>b.onclick=e=>{e.stopPropagation();closeDrawer();document.dispatchEvent(new CustomEvent('rq:documents-open-payment',{detail:{id:b.dataset.drawerOpenPayment}}))});$('#rqDrawerOpenSelected').onclick=()=>{closeDrawer();if(selectedPayment)document.dispatchEvent(new CustomEvent('rq:documents-open-payment',{detail:{id:selectedPayment.id}}));else if(selectedDocument)document.dispatchEvent(new CustomEvent('rq:documents-open-id',{detail:{id:selectedDocument.id}}))};const payBtn=$('#rqDrawerRecordPayment');if(payBtn&&invoice)payBtn.onclick=()=>{closeDrawer();document.dispatchEvent(new CustomEvent('rq:documents-record-payment',{detail:{document:invoice,summary:currentSummary||{orderId,totalMinor:total,paidMinor:paid,balanceMinor:balance,paymentStatus:balance<=0?'paid':paid>0?'partial':'pending'}}}))}}catch(e){$('#rqDocDrawerBody').innerHTML=`<div class="rqDocDrawerLoading">${esc(e.message||'Gagal memuatkan document flow.')}</div>`}}
function bindRows(){$$('#docHistory .rqHistRow').forEach(row=>{if(row.dataset.drawerBound==='1')return;row.dataset.drawerBound='1';row.tabIndex=0;row.title='Klik untuk lihat document flow & payment timeline';row.addEventListener('click',e=>{if(e.target.closest('button,a,input,select,textarea'))return;const n=rowNumber(row);if(n)openByNumber(n)});row.addEventListener('keydown',e=>{if((e.key==='Enter'||e.key===' ')&&!e.target.closest('button,a,input,select,textarea')){e.preventDefault();const n=rowNumber(row);if(n)openByNumber(n)}})})}
function init(){ensureDrawer();bindRows();const h=$('#docHistory');if(h)new MutationObserver(()=>setTimeout(bindRows,0)).observe(h,{childList:true,subtree:true})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();