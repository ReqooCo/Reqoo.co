(()=>{
'use strict';
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
let cfg={},products=[],orders=[],docs=[],editId=null,images=[],variations=[],addons=[],customs=[];
const money=m=>'RM'+(Number(m||0)/100).toFixed(2),arr=v=>{try{const x=typeof v==='string'?JSON.parse(v):v;return Array.isArray(x)?x:[]}catch{return[]}},esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const api=()=>((cfg.apiUrl||'https://api.reqoo.co').replace(/\/$/,''));
function saveCfg(){localStorage.setItem('reqoo_admin_cfg',JSON.stringify(cfg))}
async function req(path,opt={}){const h={...(opt.headers||{})};if(cfg.adminKey)h['X-Admin-Key']=cfg.adminKey.trim();if(!(opt.body instanceof FormData))h['content-type']='application/json';const r=await fetch(api()+path,{...opt,headers:h});let j={};try{j=await r.json()}catch{}if(!r.ok)throw Error(j.error||`HTTP ${r.status}`);return j}
async function adminLogin(key){key=(key||'').trim();const error=$('#gateError');if(!key){if(error){error.textContent='Masukkan Admin Key.';error.classList.remove('gate-hidden')}return false}try{const r=await fetch(api()+'/api/admin/products',{headers:{'X-Admin-Key':key}});let j={};try{j=await r.json()}catch{}if(!r.ok)throw Error(j.error||`HTTP ${r.status}`);cfg={...cfg,apiUrl:cfg.apiUrl||'https://api.reqoo.co',adminKey:key};sessionStorage.setItem('reqoo_admin_key',key);saveCfg();$('#adminGate')?.classList.add('gate-hidden');$('#adminApp')?.classList.remove('gate-hidden');return true}catch(e){if(error){error.textContent=e.message==='Unauthorized'?'Admin Key salah.':e.message;error.classList.remove('gate-hidden')}return false}}
async function start(){try{cfg=JSON.parse(localStorage.getItem('reqoo_admin_cfg')||'{}');if(!cfg||typeof cfg!=='object')cfg={}}catch{cfg={}}const saved=sessionStorage.getItem('reqoo_admin_key');if(saved){cfg.adminKey=saved;if(await adminLogin(saved)){bind();return}}$('#adminApp')?.classList.add('gate-hidden');$('#adminGate')?.classList.remove('gate-hidden');const input=$('#gateAdminKey'),button=$('#gateLogin');button?.addEventListener('click',async()=>{button.disabled=true;await adminLogin(input?.value);button.disabled=false;if(cfg.adminKey)bind()});input?.addEventListener('keydown',e=>{if(e.key==='Enter')button?.click()});input?.focus()}
function msg(t){let x=$('#builderStatus');if(!x){x=document.createElement('div');x.id='builderStatus';x.className='notice';$('#productForm')?.prepend(x)}if(x)x.textContent=t}
function reset(){editId=null;images=[];variations=[];addons=[];customs=[];['pName','pCategory','pMaterial','pSku','pDesc','pPrice'].forEach(id=>{if($('#'+id))$('#'+id).value=''});if($('#pImages'))$('#pImages').value='';renderBuilder()}
function renderBuilder(){const p=$('#imagePreview');if(p)p.innerHTML=images.length?images.map((u,i)=>`<div class="image-item"><img src="${esc(u)}"><button type="button" data-rimg="${i}">×</button>${i?'':'<small>Cover</small>'}</div>`).join(''):'<div class="muted">Belum ada gambar.</div>';const v=$('#variationList');if(v)v.innerHTML=variations.length?variations.map((x,i)=>`<div class="builder-row"><input class="input" data-vn="${i}" value="${esc(x.name)}" placeholder="Saiz"><label class="check"><input type="checkbox" data-vr="${i}" ${x.required?'checked':''}> Wajib</label><button type="button" class="button small danger" data-rv="${i}">Padam</button><div class="option-list">${arr(x.options).map((o,j)=>`<div class="option-row"><input class="input" data-on="${i}:${j}" value="${esc(o.name)}" placeholder="A5"><input class="input" type="number" min="0" step="0.01" data-op="${i}:${j}" value="${(Number(o.price_minor||0)/100).toFixed(2)}" placeholder="30"><button type="button" class="button small danger" data-ro="${i}:${j}">×</button></div>`).join('')}<button type="button" class="button small" data-ao="${i}">+ Pilihan</button></div></div>`).join(''):'<div class="muted">Tiada variation.</div>';const a=$('#addonList');if(a)a.innerHTML=addons.length?addons.map((x,i)=>`<div class="builder-row option-row"><input class="input" data-an="${i}" value="${esc(x.name)}" placeholder="Jam"><input class="input" type="number" min="0" step="0.01" data-ap="${i}" value="${(Number(x.price_minor||0)/100).toFixed(2)}" placeholder="15"><label class="check"><input type="checkbox" data-ar="${i}" ${x.required?'checked':''}> Wajib</label><button type="button" class="button small danger" data-ra="${i}">×</button></div>`).join(''):'<div class="muted">Tiada add-on.</div>';const c=$('#customList');if(c)c.innerHTML=customs.length?customs.map((x,i)=>`<div class="builder-row option-row"><input class="input" data-cn="${i}" value="${esc(x.name)}" placeholder="Nama"><select class="input" data-ct="${i}"><option value="text" ${x.type==='text'?'selected':''}>Text</option><option value="number" ${x.type==='number'?'selected':''}>Nombor</option><option value="date" ${x.type==='date'?'selected':''}>Tarikh</option></select><label class="check"><input type="checkbox" data-cr="${i}" ${x.required?'checked':''}> Wajib</label><button type="button" class="button small danger" data-rc="${i}">×</button></div>`).join(''):'<div class="muted">Tiada ruang custom.</div>'}
function sync(){ $$('[data-vn]').forEach(x=>{if(variations[x.dataset.vn])variations[x.dataset.vn].name=x.value.trim()});$$('[data-vr]').forEach(x=>{if(variations[x.dataset.vr])variations[x.dataset.vr].required=x.checked});$$('[data-on]').forEach(x=>{const[i,j]=x.dataset.on.split(':');if(variations[i]?.options[j])variations[i].options[j].name=x.value.trim()});$$('[data-op]').forEach(x=>{const[i,j]=x.dataset.op.split(':');if(variations[i]?.options[j])variations[i].options[j].price_minor=Math.max(0,Math.round(Number(x.value||0)*100))});$$('[data-an]').forEach(x=>{if(addons[x.dataset.an])addons[x.dataset.an].name=x.value.trim()});$$('[data-ap]').forEach(x=>{if(addons[x.dataset.ap])addons[x.dataset.ap].price_minor=Math.max(0,Math.round(Number(x.value||0)*100))});$$('[data-ar]').forEach(x=>{if(addons[x.dataset.ar])addons[x.dataset.ar].required=x.checked});$$('[data-cn]').forEach(x=>{if(customs[x.dataset.cn])customs[x.dataset.cn].name=x.value.trim()});$$('[data-ct]').forEach(x=>{if(customs[x.dataset.ct])customs[x.dataset.ct].type=x.value});$$('[data-cr]').forEach(x=>{if(customs[x.dataset.cr])customs[x.dataset.cr].required=x.checked})}
function renderProducts(){const l=$('#productList');if(!l)return;l.innerHTML=products.length?`<table class="table"><thead><tr><th>Produk</th><th>Harga</th><th></th></tr></thead><tbody>${products.map(p=>`<tr><td>${arr(p.images)[0]?`<img src="${esc(arr(p.images)[0])}" style="width:46px;height:46px;object-fit:cover;border-radius:8px;vertical-align:middle;margin-right:8px">`:''}<b>${esc(p.name)}</b><div class="muted">${esc(p.sku||'')} · ${esc(p.material||'')}</div></td><td>${arr(p.variations).length?money(arr(p.variations)[0]?.options?.[0]?.price_minor||0)+' · '+arr(p.variations).length+' variation':money(p.base_price_minor)}</td><td><button type="button" class="button small" data-edit="${esc(p.id)}">Edit</button> <button type="button" class="button small danger" data-del="${esc(p.id)}">Padam</button></td></tr>`).join('')}</tbody></table>`:'<div class="empty">Belum ada produk.</div>';if($('#statProducts'))$('#statProducts').textContent=products.length}
function renderOrders(){const l=$('#orderList');if(!l)return;const pay=x=>x==='paid'?'Paid':x==='failed'?'Failed':'Pending',st=x=>x==='processing'?'Processing':x==='ready'?'Ready':x==='shipped'?'Shipped':x==='completed'?'Completed':'Pending';l.innerHTML=orders.length?orders.map((o,i)=>`<div class="card" style="margin:10px 0;padding:16px"><div class="topbar"><div><b>${esc(o.order_no)}</b><div class="muted">${esc(o.name)} · ${esc(o.phone)} · ${esc(o.email)}</div><div class="muted">${new Date(o.created_at).toLocaleString('ms-MY')} · ${money(o.total_minor)}</div></div><div><span class="badge">${pay(o.payment_status)}</span> <span class="badge">${st(o.order_status)}</span></div></div><div style="margin:10px 0">${arr(o.items).map(x=>`<div>${esc(x.name)} × ${x.qty||1} <span class="muted">${esc(x.config||'')}</span></div>`).join('')}</div><div class="form-grid"><div class="field"><label>Bayaran</label><select class="input" data-opay="${i}"><option value="pending" ${o.payment_status==='pending'?'selected':''}>Pending</option><option value="paid" ${o.payment_status==='paid'?'selected':''}>Paid</option><option value="failed" ${o.payment_status==='failed'?'selected':''}>Failed</option></select></div><div class="field"><label>Status order</label><select class="input" data-ostat="${i}"><option value="pending" ${o.order_status==='pending'?'selected':''}>Pending</option><option value="processing" ${o.order_status==='processing'?'selected':''}>Processing</option><option value="ready" ${o.order_status==='ready'?'selected':''}>Ready</option><option value="shipped" ${o.order_status==='shipped'?'selected':''}>Shipped</option><option value="completed" ${o.order_status==='completed'?'selected':''}>Completed</option></select></div><div class="field"><label>No. tracking</label><input class="input" data-track="${i}" value="${esc(o.tracking_no||'')}" placeholder="J&T / Pos Laju tracking"></div></div><div class="actions" style="margin-top:10px"><button class="button primary" data-saveorder="${i}">Simpan Order</button><button class="button" data-pack="${i}">Packing Slip</button><button class="button" data-makedoc="quotation:${i}">Quotation</button><button class="button" data-makedoc="invoice:${i}">Invoice</button><button class="button" data-makedoc="receipt:${i}">Receipt</button></div></div>`).join(''):'<div class="empty">Belum ada order.</div>';if($('#statOrders'))$('#statOrders').textContent=orders.length}
function renderDocs(){['quotation','invoice','receipt'].forEach(t=>{const l=$('#'+t+'List');if(!l)return;const rows=docs.filter(d=>d.type===t);l.innerHTML=rows.length?rows.map(d=>`<div class="doc-row"><div><b>${esc(d.number)}</b><div class="muted">${esc(d.customer)} · ${money(d.total_minor)}${d.order_id?' · '+esc(d.order_id):''}</div></div><span>${esc(d.status)}</span><button type="button" class="button small" data-print="${esc(d.id)}">Cetak</button></div>`).join(''):'<div class="empty">Belum ada dokumen.</div>'})}
function tab(t){$$('.panel').forEach(x=>x.classList.add('hidden'));$('#'+t)?.classList.remove('hidden');$$('.side-btn').forEach(x=>x.classList.toggle('active',x.dataset.tab===t));if(t==='orders')loadOrders();if(['quotation','invoice','receipt'].includes(t))loadDocs()}
async function loadProducts(){try{const j=await req('/api/admin/products');products=Array.isArray(j.products)?j.products:[];renderProducts()}catch(e){msg('API produk gagal: '+e.message)}}
async function loadOrders(){try{const j=await req('/api/admin/orders');orders=Array.isArray(j.orders)?j.orders:[];renderOrders()}catch(e){const l=$('#orderList');if(l)l.innerHTML='<div class="empty">Gagal memuatkan order: '+esc(e.message)+'</div>'}}
async function loadDocs(){try{const j=await req('/api/admin/documents');docs=Array.isArray(j.documents)?j.documents:[];renderDocs();['quotation','invoice','receipt'].forEach(t=>{const n=docs.filter(d=>d.type===t).length;const x=$('#stat'+t[0].toUpperCase()+t.slice(1));if(x)x.textContent=n})}catch(e){console.error(e)}}
async function upload(files){for(const f of [...files].slice(0,8-images.length)){if(!f.type.startsWith('image/'))continue;if(f.size>10*1024*1024)continue;try{const fd=new FormData();fd.append('file',f);const j=await req('/api/admin/media',{method:'POST',body:fd});if(j?.url){images.push(j.url);renderBuilder()}}catch(e){msg('Upload gagal: '+e.message)}}if($('#uploadStatus'))$('#uploadStatus').textContent=images.length+' gambar dimuat naik.'}
async function saveProduct(){sync();const p={id:editId||crypto.randomUUID(),name:($('#pName')?.value||'').trim(),category:($('#pCategory')?.value||'').trim(),material:($('#pMaterial')?.value||'').trim(),sku:($('#pSku')?.value||'').trim(),description:($('#pDesc')?.value||'').trim(),base_price_minor:Math.max(0,Math.round(Number($('#pPrice')?.value||0)*100)),images,variations,addons,custom_fields:customs,status:'active'};if(!p.name)return msg('Isi nama produk.');if(!variations.length&&p.base_price_minor<=0)return msg('Masukkan harga produk.');try{await req('/api/admin/products',{method:'POST',body:JSON.stringify(p)});msg('Produk berjaya disimpan.');await loadProducts();$('#productForm')?.classList.add('hidden');reset()}catch(e){msg('Simpan gagal: '+e.message)}}
async function saveOrder(i){const o=orders[i];if(!o)return;const d={payment_status:$(`[data-opay="${i}"]`)?.value,order_status:$(`[data-ostat="${i}"]`)?.value,tracking_no:$(`[data-track="${i}"]`)?.value||''};try{await req('/api/admin/orders/'+encodeURIComponent(o.id),{method:'PUT',body:JSON.stringify(d)});await loadOrders()}catch(e){alert(e.message)}}
function docNumber(type){return (type==='quotation'?'QUO':type==='invoice'?'INV':'REC')+'-'+Date.now().toString().slice(-8)}
async function makeDoc(type,i){const o=orders[i];if(!o)return;const d={type,number:docNumber(type),customer:o.name,email:o.email,items:arr(o.items),total_minor:Number(o.total_minor||0),status:type==='receipt'&&o.payment_status==='paid'?'issued':'draft',order_id:o.id};try{await req('/api/admin/documents',{method:'POST',body:JSON.stringify(d)});await loadDocs();alert(`${type.toUpperCase()} berjaya dibuat untuk ${o.order_no}.`)}catch(e){alert(e.message)}}
function printOrder(o){const items=arr(o.items);const w=window.open('','_blank');if(!w)return alert('Popup disekat.');w.document.write(`<html><head><title>${esc(o.order_no)}</title><style>body{font-family:Arial;padding:35px;max-width:760px;margin:auto}table{width:100%;border-collapse:collapse}td,th{padding:8px;border-bottom:1px solid #ddd;text-align:left}</style></head><body><h1>AB ART TRADING</h1><h2>PACKING SLIP</h2><p>Order: <b>${esc(o.order_no)}</b><br>Pelanggan: ${esc(o.name)}<br>Telefon: ${esc(o.phone)}<br>Email: ${esc(o.email)}</p><table><tr><th>Item</th><th>Qty</th><th>Jumlah</th></tr>${items.map(x=>`<tr><td>${esc(x.name)}<br><small>${esc(x.config||'')}</small></td><td>${x.qty||1}</td><td>${money(Number(x.total_minor||0)*Number(x.qty||1))}</td></tr>`).join('')}</table><h2>Jumlah: ${money(o.total_minor)}</h2><p>Tracking: ${esc(o.tracking_no||'-')}</p><script>window.onload=()=>print()</script></body></html>`);w.document.close()}
function printDoc(id){const d=docs.find(x=>x.id===id);if(!d)return;const items=arr(d.items);const w=window.open('','_blank');if(!w)return alert('Popup disekat.');w.document.write(`<html><head><title>${esc(d.number)}</title><style>body{font-family:Arial;padding:35px;max-width:760px;margin:auto}table{width:100%;border-collapse:collapse}td,th{padding:8px;border-bottom:1px solid #ddd;text-align:left}</style></head><body><h1>AB ART TRADING</h1><h2>${esc(d.type.toUpperCase())}</h2><p>No: <b>${esc(d.number)}</b><br>Pelanggan: ${esc(d.customer)}<br>Email: ${esc(d.email||'')}</p><table><tr><th>Item</th><th>Qty</th><th>Jumlah</th></tr>${items.map(x=>`<tr><td>${esc(x.name)}<br><small>${esc(x.config||'')}</small></td><td>${x.qty||1}</td><td>${money(Number(x.total_minor||0)*Number(x.qty||1))}</td></tr>`).join('')}</table><h2>Jumlah: ${money(d.total_minor)}</h2><p>Status: ${esc(d.status)}</p><script>window.onload=()=>print()</script></body></html>`);w.document.close()}
function bind(){if($('#apiUrl'))$('#apiUrl').value=cfg.apiUrl||'https://api.reqoo.co';if($('#adminKey'))$('#adminKey').value=cfg.adminKey||'';if($('#qrUrl'))$('#qrUrl').value=cfg.qrUrl||'';renderBuilder();$('#pImages')?.addEventListener('change',e=>upload(e.target.files));document.addEventListener('click',async e=>{const b=e.target.closest('button,a');if(!b)return;if(b.classList.contains('side-btn')){e.preventDefault();tab(b.dataset.tab)}else if(b.id==='newProduct'){reset();$('#productForm')?.classList.remove('hidden');tab('products')}else if(b.id==='cancelProduct'){reset();$('#productForm')?.classList.add('hidden')}else if(b.id==='addVariation'){sync();variations.push({name:'Saiz',required:true,options:[{name:'A5',price_minor:0}]});renderBuilder()}else if(b.id==='addAddon'){sync();addons.push({name:'Add-on',price_minor:0,required:false});renderBuilder()}else if(b.id==='addCustom'){sync();customs.push({name:'Nama',type:'text',required:true});renderBuilder()}else if(b.id==='saveProduct'){await saveProduct()}else if(b.dataset.rimg!==undefined){images.splice(+b.dataset.rimg,1);renderBuilder()}else if(b.dataset.rv!==undefined){sync();variations.splice(+b.dataset.rv,1);renderBuilder()}else if(b.dataset.ao!==undefined){sync();variations[+b.dataset.ao]?.options.push({name:'Pilihan',price_minor:0});renderBuilder()}else if(b.dataset.ro!==undefined){sync();const[i,j]=b.dataset.ro.split(':');variations[i]?.options.splice(+j,1);renderBuilder()}else if(b.dataset.ra!==undefined){sync();addons.splice(+b.dataset.ra,1);renderBuilder()}else if(b.dataset.rc!==undefined){sync();customs.splice(+b.dataset.rc,1);renderBuilder()}else if(b.dataset.edit!==undefined){const p=products.find(x=>String(x.id)===String(b.dataset.edit));if(!p)return;editId=p.id;images=arr(p.images);variations=arr(p.variations);addons=arr(p.addons);customs=arr(p.custom_fields);[['pName',p.name],['pCategory',p.category],['pMaterial',p.material],['pSku',p.sku],['pDesc',p.description],['pPrice',(Number(p.base_price_minor||0)/100).toFixed(2)]].forEach(([id,v])=>{if($('#'+id))$('#'+id).value=v||''});renderBuilder();$('#productForm')?.classList.remove('hidden');tab('products')}else if(b.dataset.del!==undefined){if(!confirm('Padam produk ini?'))return;try{await req('/api/admin/products/'+encodeURIComponent(b.dataset.del),{method:'DELETE'});await loadProducts()}catch(e){alert(e.message)}}else if(b.dataset.saveorder!==undefined){await saveOrder(+b.dataset.saveorder)}else if(b.dataset.pack!==undefined){printOrder(orders[+b.dataset.pack])}else if(b.dataset.makedoc!==undefined){const[t,i]=b.dataset.makedoc.split(':');await makeDoc(t,+i)}else if(b.dataset.print!==undefined){printDoc(b.dataset.print)}else if(b.dataset.doc!==undefined){const customer=prompt('Nama pelanggan?');if(!customer)return;const total=Number(prompt('Jumlah (RM)?','0')||0);if(!Number.isFinite(total)||total<0)return;await req('/api/admin/documents',{method:'POST',body:JSON.stringify({type:b.dataset.doc,number:docNumber(b.dataset.doc),customer,email:'',items:[],total_minor:Math.round(total*100),status:'draft'})});await loadDocs()}else if(b.id==='saveSettings'){cfg.apiUrl=$('#apiUrl')?.value.trim()||'https://api.reqoo.co';cfg.adminKey=$('#adminKey')?.value.trim()||cfg.adminKey;cfg.qrUrl=$('#qrUrl')?.value.trim()||'';saveCfg();try{await req('/api/admin/settings',{method:'POST',body:JSON.stringify({qr_url:cfg.qrUrl})});alert('Tetapan disimpan.')}catch(e){alert(e.message)}}});loadProducts();loadOrders();loadDocs()}
start();
})();